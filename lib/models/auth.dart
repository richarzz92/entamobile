class AuthModel {
  String? token, employeeName, photoProfile;
  String? companyCode, host, username;
  AuthModel(
      {this.token,
      this.username,
      this.employeeName,
      this.photoProfile,
      this.companyCode,
      this.host});
}
